<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liuhappy.BugDemo.mapper.ProductMapper">
    <select id="selectMaxPdId" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT
            IFNULL(MAX(right(PD.PD_ID,CHAR_LENGTH(PD.PD_ID)-2)) + 1,#{pdId})
        FROM pd_m PD
        WHERE PD.PD_ID LIKE CONCAT('%', DATE_FORMAT(NOW(),'%Y'), '%')
    </select>
</mapper>
